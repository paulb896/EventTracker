Event Tracker
Author: Paul Beauchamp
Date: April 19 2012

Dependencies:
 * Must have php with MongoDB drivers installed:
     http://php.net/manual/en/mongo.installation.php

Contains classes and test scripts associated with event tracking.

Usage:

--- Save an event ---
   $eventData = array('dateTime' => date("Y-m-d H:i:s"));
   $event = new DataAccessor_Event();
   $event->saveEvent($eventData);
